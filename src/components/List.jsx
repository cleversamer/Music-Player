import styled from "styled-components";

const List = ({ open, musicNum, onMusicChange, onOpen, onClose }) => {
  return (
    <Container>
      <Header></Header>
    </Container>
  );
};

const Container = styled.div``;

const Header = styled.div``;

export default List;
